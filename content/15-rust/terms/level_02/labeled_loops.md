# Labeled Loops (`'label: loop`)

> **Level 2 — Rust**
> Naming a loop with a label so that nested `break` and `continue` can target an outer loop by name, not just the innermost one.

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **Language Fundamental**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** Naming a loop with a label so that nested `break` and `continue` can target an outer loop by name, not just the innermost one.

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
