# Advanced Pattern Features

> **Level 2 — Rust**
> The extended `match` sub-grammar: match guards (`if cond`), `@` bindings, or-patterns (`A | B`), range patterns, `ref`, and `...` rest patterns.

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **Language Fundamental**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** The extended `match` sub-grammar: match guards (`if cond`), `@` bindings, or-patterns (`A | B`), range patterns, `ref`, and `...` rest patterns.

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
