# `Waker` and `Context`

> **Level 10 — Rust**
> `std::task::Waker` and `std::task::Context` — the core signalling mechanism async tasks use to notify an executor that a paused `Future` is ready to be polled again.

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **Async Programming**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** `std::task::Waker` and `std::task::Context` — the core signalling mechanism async tasks use to notify an executor that a paused `Future` is ready to be polled again.

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
