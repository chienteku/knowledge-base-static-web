# `Cow` for API Flexibility

> **Level 18 — Rust**
> Using `Cow<'_, str>` or `Cow<'_, [T]>` in APIs to accept both owned and borrowed data, cloning only when mutation is needed.

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **Design Pattern**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** Using `Cow<'_, str>` or `Cow<'_, [T]>` in APIs to accept both owned and borrowed data, cloning only when mutation is needed.

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
