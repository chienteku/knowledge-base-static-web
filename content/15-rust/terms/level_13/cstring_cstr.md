# `CString` / `CStr`

> **Level 13 — Rust**
> Nul-terminated string types for safely passing text data across the C FFI boundary: `CString` (owned) and `CStr` (borrowed view).

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **FFI / Systems**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** Nul-terminated string types for safely passing text data across the C FFI boundary: `CString` (owned) and `CStr` (borrowed view).

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
