# `Borrow<T>` Trait

> **Level 14 — Rust**
> Similar to `AsRef`, but additionally guarantees that the borrowed form has the same `Hash`, `Eq`, and `Ord` as the owning type — critical for `HashMap` and `BTreeMap` lookups.

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **Trait / Interface**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** Similar to `AsRef`, but additionally guarantees that the borrowed form has the same `Hash`, `Eq`, and `Ord` as the owning type — critical for `HashMap` and `BTreeMap` lookups.

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
