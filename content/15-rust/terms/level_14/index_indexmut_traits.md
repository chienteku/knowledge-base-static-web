# `Index` and `IndexMut` Traits

> **Level 14 — Rust**
> The `std::ops` traits behind the `[]` subscript operator, allowing types to define custom indexing behaviour.

---

## 1. Prerequisites
_(none — see `relationships.json` for the dependency graph)_

---

## 2. Term Category
- **Trait / Interface**

---

## 3. Environment Context
- **Rust (compiled, systems language)**

---

## 4. Explanation

> 🚧 **Stub** — This term file is a placeholder. Full content has not been written yet.
>
> **Summary:** The `std::ops` traits behind the `[]` subscript operator, allowing types to define custom indexing behaviour.

---

## 5. Related Terms
_(none — see `relationships.json` for the relationship graph)_

---

## 6. Key Takeaways
- This is a stub file. Content will be expanded in a future generation pass.
